{
  "name": "reelsong-extension",
  "version": "1.1.0",
  "description": "ReelSong - Automatic music identifier for Instagram Reels with YouTube integration",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/build-scripts.js && node scripts/copy-manifest.js",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js",
    "test:browser": "npm run build && playwright test"
  },
  "dependencies": {
    "lucide-react": "^0.350.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1",
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.1.4"
  }
}
